# Jira Issueコメント追加スクリプト

## 概要

Jira Issueにコメントを追加する専用スクリプトです。`update-issue.sh`の`--body-file`オプションを使用するとチケット本文（description）を上書きしてしまうため、コメントを追加する場合はこのスクリプトを使用してください。

## 使用方法

### 基本的な使い方

```bash
# コメントを直接入力
./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body "## 作業完了\n\n実装が完了しました"

# ファイルからコメントを読み込み
./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body-file ./comment.md

# 標準入力からコメントを読み込み
echo "## 作業完了" | ./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body-file -

# ヒアドキュメントで使用
./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body-file - << 'EOF'
## 🎉 作業完了報告

Issue MWD-38の作業が完了しました。

## 📊 実施した作業
- 実装完了
- テスト完了
EOF
```

## オプション

| オプション | 説明 | 必須 |
|-----------|------|------|
| `--issue-key KEY` | Issueキー（例: MWD-38） | ✅ |
| `--body TEXT` | コメント本文（直接入力） | ⚠️ |
| `--body-file FILE` | コメント本文をファイルから読み込み | ⚠️ |
| `--help` | ヘルプを表示 | ❌ |

**注意**: `--body` または `--body-file` のいずれかは必須です。

## 機能

- ✅ マークダウン形式のコメントをADF形式に自動変換
- ✅ ファイルまたは標準入力からコメントを読み込み
- ✅ 特殊文字（バッククォートなど）を安全に処理
- ✅ Issue存在確認
- ✅ エラーハンドリング

## マークダウンサポート

以下のマークダウン記法がサポートされています：

- **見出し**: `## 見出し2`, `### 見出し3`
- **段落**: 空行で区切られたテキスト
- **チェックボックス**: `- [ ] 未チェック`, `- [x] チェック済み`

## 例

### 例1: 作業完了報告

```bash
./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body-file - << 'EOF'
## 🎉 作業完了報告

Issue MWD-38「JIRAチケットへのコメント追加スクリプトを実装」の作業が完了しました。

## 📊 実施した作業

### 実装
- `scripts/jira/issues/comment-issue.sh` を作成
- マークダウンからADF形式への変換機能を実装
- ファイル/標準入力からのコメント読み込み機能を実装

### テスト
- ヘルプ表示の確認
- エラーハンドリングの確認

## ✅ 達成した目標

- [x] Jira Issueにコメントを追加するスクリプトを実装
- [x] `update-issue.sh`とは異なり、チケット本文を上書きしない
- [x] マークダウン形式のコメントをサポート

## 🔗 関連リンク

- **PR**: 作成予定
- **ブランチ**: `feature/MWD-109-jira`
EOF
```

### 例2: ファイルから読み込み

```bash
# comment.md を作成
cat > comment.md << 'EOF'
## レビュー対応完了

ご指摘いただいた点について、すべて対応しました。
EOF

# コメントを追加
./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body-file comment.md
```

### 例3: パイプで使用

```bash
cat comment.md | ./scripts/jira/issues/comment-issue.sh --issue-key MWD-38 --body-file -
```

## エラーハンドリング

スクリプトは以下のエラーを適切に処理します：

- Issueキーが指定されていない場合
- Issueが存在しない場合
- コメント本文が空の場合
- ファイルが見つからない場合
- Jira API認証エラー

## 注意事項

1. **認証情報の設定**: `scripts/jira/config.local.sh` に `JIRA_EMAIL` と `JIRA_API_TOKEN` を設定してください
2. **Issueキーの形式**: Issueキーは `MWD-38` のような形式である必要があります
3. **コメント本文**: 空のコメントは追加できません

## 関連スクリプト

- `update-issue.sh`: Issueのタイトルや説明を更新（本文を上書き）
- `get-issue.sh`: Issue情報を取得
- `create-issue.sh`: 新しいIssueを作成

## トラブルシューティング

### エラー: 環境変数 JIRA_EMAIL と JIRA_API_TOKEN が設定されていません

**解決方法**: `scripts/jira/config.local.sh` を作成して認証情報を設定してください。

```bash
cp scripts/jira/config.local.sh.example scripts/jira/config.local.sh
# 認証情報を編集
```

### エラー: Issue 'MWD-XXX' の情報取得に失敗しました

**原因**: Issueが存在しない、またはアクセス権限がない

**解決方法**: Issueキーが正しいか確認し、アクセス権限を確認してください。

### エラー: コメント追加に失敗しました

**原因**: Jira APIのエラー

**解決方法**: エラーメッセージを確認し、コメント本文が適切な形式か確認してください。
